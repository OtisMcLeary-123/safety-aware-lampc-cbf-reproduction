# Security

Do not commit provider tokens, API keys, environment files, raw provider
records, or private experiment data. Use local environment variables or the
ignored token files only for explicitly authorized experiments.

If you find a credential or other sensitive data in the repository history,
rotate it immediately and report the commit privately to the repository owner.
Do not open a public issue containing the secret.
